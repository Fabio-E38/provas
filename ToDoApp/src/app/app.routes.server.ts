import { ServerRoute } from '@angular/ssr';

export const serverRoutes: ServerRoute[] = [
  // Le route verranno gestite automaticamente
  // Aggiungi qui configurazioni specifiche per il server se necessario
];
